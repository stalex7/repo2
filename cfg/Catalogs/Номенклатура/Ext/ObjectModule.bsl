
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если НЕ ЭтоГруппа Тогда
		ЕдиницаИзмерения = Константы.ЕдиницаИзмеренияОсновная.Получить();
		СтранаПроизводства = Справочники.СтраныМира.Россия;
		ЕдиницаИзмеренияВес = Константы.ЕдиницаИзмеренияВес.Получить();
	КонецЕсли;
	
КонецПроцедуры
